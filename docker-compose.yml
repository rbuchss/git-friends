services:
  tester:
    build:
      context: .
      target: git-friends-tester-local
    working_dir: /cyberdyne
    volumes:
      - .:/cyberdyne
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges
    environment:
      TERM: ${TERM:-}
